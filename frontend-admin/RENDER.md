# N999Bet Employee Admin on Render

Service type: Static Site
Root Directory: `frontend-admin`
Build Command: `npm install && npm run build`
Publish Directory: `dist`
Environment:
`VITE_API_URL=https://<N999BET-BACKEND>.onrender.com`

The backend must authorize employee/admin roles. The frontend never grants permissions itself.
