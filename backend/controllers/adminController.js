const User = require('../models/User');
const Wallet = require('../models/Wallet');
const Transaction = require('../models/Transaction');
const AdminBalance = require('../models/AdminBalance');
const Game = require('../models/Game');
const pool = require('../config/database');
const GameMetadata = require('../models/GameMetadata');
const slotopolService = require('../services/slotopolService');

// ============================================================
// USER MANAGEMENT
// ============================================================
exports.getUsers = async (req, res) => { try { const { search, status, role, limit = 50, offset = 0 } = req.query; const safeLimit = Math.min(Math.max(parseInt(limit,10)||50,1),100); const safeOffset = Math.max(parseInt(offset,10)||0,0); const users = await User.getAll({search,status,role,limit:safeLimit,offset:safeOffset}); const withBalance = await Promise.all(users.map(async u => { const w=await Wallet.findByUserId(u.id); return {...u,balance:w?parseFloat(w.main_balance):0}; })); res.json({success:true,users:withBalance}); } catch(error){console.error('Get users error:',error);res.status(500).json({success:false,error:'Failed to fetch users'});} };
exports.getUserDetails = async (req,res)=>{try{const user=await User.findById(req.params.id);if(!user)return res.status(404).json({success:false,error:'User not found'});const wallet=await Wallet.findByUserId(req.params.id);const transactions=await Transaction.findByUserId(req.params.id,20);res.json({success:true,user,wallet,recentTransactions:transactions});}catch(error){console.error('Get user details error:',error);res.status(500).json({success:false,error:'Failed to fetch user details'});}};
exports.updateUserStatus = async (req,res)=>{try{const{id}=req.params;const{status,reason}=req.body;const allowed=['active','inactive','blocked','suspended'];if(!allowed.includes(status))return res.status(400).json({success:false,error:'Invalid user status'});const user=await User.findById(id);if(!user)return res.status(404).json({success:false,error:'User not found'});await User.update(id,{status});const activityLog=require('../models/AdminActivityLog');await activityLog.create({adminId:req.userId,action:'UPDATE_USER_STATUS',targetType:'user',targetId:id,description:`Status changed to ${status}${reason?`: ${reason}`:''}`});res.json({success:true,message:`User status updated to ${status}`});}catch(error){console.error('Update user status error:',error);res.status(500).json({success:false,error:'Failed to update user status'});}};
exports.deleteUser = async(req,res)=>{try{const{id}=req.params;const{reason}=req.body;const user=await User.findById(id);if(!user)return res.status(404).json({success:false,error:'User not found'});await User.update(id,{status:'blocked'});const activityLog=require('../models/AdminActivityLog');await activityLog.create({adminId:req.userId,action:'DELETE_USER',targetType:'user',targetId:id,description:`User blocked${reason?`: ${reason}`:''}`});res.json({success:true,message:'User blocked'});}catch(error){console.error('Delete user error:',error);res.status(500).json({success:false,error:'Failed to block user'});}};
exports.adjustUserBalance = async(req,res)=>{try{const{id}=req.params;const numericAmount=Number(req.body.amount);const type=req.body.type||'adjustment';if(!Number.isFinite(numericAmount)||numericAmount===0)return res.status(400).json({success:false,error:'Amount must be a non-zero number'});const user=await User.findById(id);if(!user)return res.status(404).json({success:false,error:'User not found'});const wallet=await Wallet.findByUserId(id);if(!wallet)return res.status(404).json({success:false,error:'Wallet not found'});if(numericAmount<0&&Number(wallet.main_balance)<Math.abs(numericAmount))return res.status(400).json({success:false,error:'Insufficient user balance'});const before=Number(wallet.main_balance);await Wallet.updateBalance(id,'main',numericAmount);await Transaction.create({userId:id,type,amount:numericAmount,beforeBalance:before,afterBalance:before+numericAmount,walletType:'main',status:'completed',description:`${type} adjustment by admin`,metadata:{adminId:req.userId}});res.json({success:true,message:'Balance adjusted successfully'});}catch(error){console.error('Adjust user balance error:',error);res.status(500).json({success:false,error:'Failed to adjust balance'});}};

// ============================================================
// TRANSACTION MANAGEMENT
// ============================================================
exports.getTransactions=async(req,res)=>{try{const{status,type,limit=50,offset=0}=req.query;const safeLimit=Math.min(Math.max(parseInt(limit,10)||50,1),100);const safeOffset=Math.max(parseInt(offset,10)||0,0);let transactions;if(status)transactions=await Transaction.findByStatus(status,safeLimit);else{const[rows]=await pool.query(`SELECT t.*, u.username as user_name FROM user_transactions t LEFT JOIN users u ON t.user_id = u.id ${type?'WHERE t.type = ?':''} ORDER BY t.created_at DESC LIMIT ? OFFSET ?`,type?[type,safeLimit,safeOffset]:[safeLimit,safeOffset]);transactions=rows;}res.json({success:true,transactions});}catch(error){console.error('Get transactions error:',error);res.status(500).json({success:false,error:'Failed to fetch transactions'});}};
exports.approveTransaction=async(req,res)=>{const connection=await pool.getConnection();try{const id=Number(req.params.id);if(!Number.isInteger(id)||id<=0){connection.release();return res.status(400).json({success:false,error:'Invalid transaction id'});}await connection.beginTransaction();const[rows]=await connection.query('SELECT * FROM user_transactions WHERE id = ? FOR UPDATE',[id]);const tx=rows[0];if(!tx){await connection.rollback();return res.status(404).json({success:false,error:'Transaction not found'});}if(tx.status!=='pending'){await connection.rollback();return res.status(400).json({success:false,error:'Transaction is no longer pending'});}const amount=Number(tx.amount);if(!Number.isFinite(amount)||amount<=0){await connection.rollback();return res.status(400).json({success:false,error:'Invalid transaction amount'});}const[adminRows]=await connection.query('SELECT balance FROM admin_balances WHERE admin_id = ? FOR UPDATE',[req.userId]);if(!adminRows[0]){await connection.rollback();return res.status(400).json({success:false,error:'Admin balance not found'});}if(tx.type==='deposit'){if(Number(adminRows[0].balance)<amount){await connection.rollback();return res.status(400).json({success:false,error:'Insufficient admin/master balance for deposit approval'});}const[walletRows]=await connection.query('SELECT main_balance FROM wallets WHERE user_id = ? FOR UPDATE',[tx.user_id]);if(!walletRows[0]){await connection.rollback();return res.status(404).json({success:false,error:'Player wallet not found'});}const before=Number(walletRows[0].main_balance);await connection.query('UPDATE wallets SET main_balance = main_balance + ? WHERE user_id = ?',[amount,tx.user_id]);await connection.query('UPDATE admin_balances SET balance = balance - ?, updated_at = NOW() WHERE admin_id = ?',[amount,req.userId]);await connection.query('UPDATE user_transactions SET status = ?, before_balance = ?, after_balance = ?, approved_by = ?, approved_at = NOW() WHERE id = ? AND status = ?',['completed',before,before+amount,req.userId,id,'pending']);}else if(tx.type==='withdraw'){const[walletRows]=await connection.query('SELECT main_balance FROM wallets WHERE user_id = ? FOR UPDATE',[tx.user_id]);if(!walletRows[0]||Number(walletRows[0].main_balance)<amount){await connection.rollback();return res.status(400).json({success:false,error:'Insufficient player balance for withdrawal'});}await connection.query('UPDATE wallets SET main_balance = main_balance - ? WHERE user_id = ? AND main_balance >= ?',[amount,tx.user_id,amount]);await connection.query('UPDATE admin_balances SET balance = balance + ?, updated_at = NOW() WHERE admin_id = ?',[amount,req.userId]);await connection.query('UPDATE user_transactions SET status = ?, before_balance = ?, after_balance = ?, approved_by = ?, approved_at = NOW() WHERE id = ? AND status = ?',['completed',Number(walletRows[0].main_balance),Number(walletRows[0].main_balance)-amount,req.userId,id,'pending']);}else{await connection.rollback();return res.status(400).json({success:false,error:'Unsupported transaction type for approval'});}await connection.commit();return res.json({success:true,message:'Transaction approved'});}catch(error){try{await connection.rollback();}catch(_){}console.error('Approve transaction error:',error);return res.status(500).json({success:false,error:'Approval failed'});}finally{connection.release();}};
exports.rejectTransaction=async(req,res)=>{try{const id=Number(req.params.id);const{reason}=req.body;const connection=await pool.getConnection();await connection.beginTransaction();try{const[result]=await connection.query('UPDATE user_transactions SET status = ?, approved_by = ?, approved_at = NOW(), description = CONCAT(COALESCE(description, ""), ?) WHERE id = ? AND status = ?',['rejected',req.userId,reason?` | Rejected: ${String(reason).slice(0,500)}`:'',id,'pending']);if(!result.affectedRows){await connection.rollback();return res.status(400).json({success:false,error:'Transaction not found or no longer pending'});}await connection.commit();return res.json({success:true,message:'Transaction rejected'});}catch(err){await connection.rollback();throw err;}finally{connection.release();}}catch(error){console.error('Reject transaction error:',error);res.status(500).json({success:false,error:'Reject failed'});}};

// ============================================================
// GAME MANAGEMENT
// ============================================================
exports.getGames=async(req,res)=>{try{let games=await slotopolService.getGameList();if(!Array.isArray(games))games=games.list||games.data||[];const metadata=await GameMetadata.find();const metaMap={};metadata.forEach(m=>metaMap[m.gameId]=m);const enriched=games.map(game=>{const meta=metaMap[game.ID]||{};return{id:game.ID,name:game.Name||game.name||'Unknown',provider:game.Prov||game.provider||'unknown',isActive:meta.isActive!==false,minBet:meta.minBet||0.1,maxBet:meta.maxBet||100,difficulty:meta.difficulty||'medium',order:meta.order||0,tags:meta.tags||[]};});enriched.sort((a,b)=>(a.order||0)-(b.order||0));res.json({success:true,games:enriched});}catch(error){console.error('Get games error:',error);res.status(500).json({success:false,error:'Failed to get games'});}};
exports.getGameById=async(req,res)=>{try{const gameId=req.params.id;let games=await slotopolService.getGameList();if(!Array.isArray(games))games=games.list||games.data||[];const slotopolGame=games.find(g=>String(g.ID)===String(gameId));if(!slotopolGame)return res.status(404).json({success:false,error:'Game not found'});const meta=await GameMetadata.findOne({gameId});const game={id:slotopolGame.ID,name:slotopolGame.Name||slotopolGame.name,provider:slotopolGame.Prov||slotopolGame.provider||'unknown',isActive:meta?.isActive!==false,minBet:meta?.minBet||0.1,maxBet:meta?.maxBet||100,difficulty:meta?.difficulty||'medium',order:meta?.order||0,tags:meta?.tags||[]};res.json({success:true,game});}catch(error){console.error('Get game by id error:',error);res.status(500).json({success:false,error:'Failed to get game'});}};
exports.createGame=async(req,res)=>{try{const{gameId,...metadata}=req.body;if(!gameId)return res.status(400).json({success:false,error:'gameId is required'});if(metadata.minBet!=null&&metadata.maxBet!=null&&Number(metadata.minBet)>Number(metadata.maxBet))return res.status(400).json({success:false,error:'minBet cannot exceed maxBet'});delete metadata.rtpOverride;delete metadata.winRate;const updated=await GameMetadata.findOneAndUpdate({gameId},metadata,{upsert:true,new:true});res.status(201).json({success:true,data:updated});}catch(error){console.error('Create game metadata error:',error);res.status(500).json({success:false,error:'Failed to create game metadata'});}};
exports.updateGame=async(req,res)=>{try{const{id}=req.params;const{isActive,minBet,maxBet,difficulty,order,tags}=req.body;if(minBet!=null&&maxBet!=null&&Number(minBet)>Number(maxBet))return res.status(400).json({success:false,error:'minBet cannot exceed maxBet'});const updated=await GameMetadata.findOneAndUpdate({gameId:id},{isActive,minBet,maxBet,difficulty,order,tags},{upsert:true,new:true});res.json({success:true,data:updated});}catch(error){console.error('Update game error:',error);res.status(500).json({success:false,error:'Failed to update game'});}};
exports.deleteGame=async(req,res)=>{try{const updated=await GameMetadata.findOneAndUpdate({gameId:req.params.id},{isActive:false},{upsert:true,new:true});res.json({success:true,message:'Game deactivated',data:updated});}catch(error){console.error('Delete game error:',error);res.status(500).json({success:false,error:'Failed to deactivate game'});}};
